const HUD = () => {
  return (
    <div
      style={{
        position: "absolute",
        top: 0,
        left: 0,
        width: "100%",
        pointerEvents: "none",
        padding: "1rem",
        color: "white",
        fontFamily: "sans-serif",
      }}
    >
      <div>0 - 0</div>
    </div>
  );
};

export default HUD;
